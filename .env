# Airflow UID – on macOS the warning about AIRFLOW_UID can be safely ignored,
# but setting it avoids the warning. Use 50000 (the image default) on macOS/Windows.
# On Linux: run `echo "AIRFLOW_UID=$(id -u)" > .env` instead.
AIRFLOW_UID=50000
