apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-dags-pvc
spec:
  accessModes:
    - ReadWriteMany  # or ReadWriteOnce depending on your storage class
  resources:
    requests:
      storage: 1Gi
