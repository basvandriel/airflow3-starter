apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-logs-pvc
  namespace: airflow-dev
spec:
  accessModes:
    - ReadWriteMany  # choose according to your storage class; RWM is convenient
  resources:
    requests:
      storage: 1Gi
